import React from 'react'
import styled from 'styled-components'

const MenuWrapper = styled.div`
  display: flex;
  flex-direction: column;
  background-color: #b48ead;
  width: 5.5rem;
`

const Menu = () => <MenuWrapper></MenuWrapper>

export default Menu
